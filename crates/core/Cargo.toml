[package]
name = "github-ranked-core"
description = "Ranking math and card rendering, shared by the server and the browser."
version.workspace = true
edition.workspace = true
license.workspace = true

# The default libtest harness would otherwise be benched alongside the criterion
# targets, and it rejects criterion's flags.
[lib]
bench = false

[dependencies]
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true
proptest = "1"
criterion = { version = "0.7", features = ["html_reports"] }

[[bench]]
name = "engine"
harness = false

[[bench]]
name = "render"
harness = false
